// js/board/board.js

// This is messy! The board needs access to the cells and vice versa

// function boardUpdate( scope ){
    

//     cell_positions.push([cells[cell].state.position.x, cells[cell].state.position.y])
        
//     var board = this;

//     var w = scope.constants.width,
//     h = scope.constants.height,
//     num_rows = scope.constants.num_rows,
//     num_columns = scope.constants.num_columns;

    
//     if (state.hasOwnProperty('board')){
//         var board = scope.state.board;
//         for(var coord in cell_positions){
//             board[coord[0]][board[coord[1]]] = true;
//         }

//     };

//     board.update = function boardUpdate{
//         var cell_positions = {}
//         for(row in scope.state.board){
//             for(column in row){

//             }
//         }

//     }


    
// }

module.exports = Board;